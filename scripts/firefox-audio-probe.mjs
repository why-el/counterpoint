import {firefox} from '@playwright/test';import {writeFile} from 'node:fs/promises';
const browser=await firefox.launch({headless:false});try{const page=await browser.newPage();await page.setContent('<button>Enable audio</button>');await page.evaluate(()=>{document.querySelector('button').onclick=async()=>{try{const ctx=new AudioContext();window.result={created:true,state:ctx.state};await ctx.resume();window.result={created:true,resumed:true,state:ctx.state};}catch(e){window.result={error:e.name,message:e.message};}};});await page.locator('button').click();await page.waitForTimeout(1000);const result=await page.evaluate(()=>window.result);console.log(JSON.stringify(result));await writeFile('evidence/firefox-audio-environment.json',JSON.stringify({version:browser.version(),...result},null,2));}finally{await browser.close();}
